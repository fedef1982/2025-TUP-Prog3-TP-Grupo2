import { Inter, Lusitana, IBM_Plex_Mono, Tangerine, Michroma,} from 'next/font/google';
import {  IBM_Plex_Sans } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

/*export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const lusitana = IBM_Plex_Mono({
  weight: ['400', '700'],
  subsets: ['latin'],
});*/

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});


export const michroma = Lusitana({
  weight: ['400', '400'],
  subsets: ['latin'],
});