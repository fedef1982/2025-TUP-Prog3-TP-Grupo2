{
  "scripts": {
    "dev": "npm-run-all --parallel dev:front dev:back",
    "dev:front": "cd frontend && pnpm run dev",
    "dev:back": "cd backend && npm run start:dev",
    "dev:test": "cd backend && npm run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
